# NOS → Φ-Lite 0.9.0 (honest map)

The attached monograph unifies D2TM, NeuroCPU, HOR-Qudit, pVRAM, and a 144-function pharmacology catalogue as one fiber bundle. **This build does not implement that object.**

Kept as *simulation knobs*:
- Sophia oscillator `Ö + φ⁻¹ Ȯ + ω₀² O = F_paradox` with F from pred_err + novelty + intent_gap
- Efficiency Ξ with a **cap 0.92** (no U36 sovereignty / no Ξ=0.999 emission gate)
- Abstract occupancies R2A, NMDA, GABA, CB, ENT — names only, not binding curves for real compounds
- Mode rotation sober/entropy/couple/damp every 48 cycles
- Voxel swarm + paradox inject already in 0.8.6

Dropped on purpose:
- DeepSeek API / logit-bias “hardware hack”
- Real-substance operators (LSD, DMT, …), PK, dosing
- Casimir / 130 Hz GPU claims
- Demiurgic barrier as physics
- Any claim that Φ-Lite is a QPU or Stage 5

Use `/qpu` and settings `qpu_mode=`.
